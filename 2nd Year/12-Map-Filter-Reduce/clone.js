// Clone the array using map function
let arr = [1, 2, 3, 4, 5];

let res = arr.map((value) => value);
console.log(res);

// Return the square of every value
res = arr.map(val => val * val);
console.log(res);